// @ts-check
import { defineConfig } from 'astro/config';

// User site (repo named <username>.github.io) — serves from the domain root,
// so no `base` is needed.
export default defineConfig({
  site: 'https://edwintieu5642.github.io',
});
